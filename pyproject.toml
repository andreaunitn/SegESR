[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "segesr"
version = "0.1.0"
dependencies = [
    "torch",
    "diffusers",
    "transformers",
    "accelerate"
]